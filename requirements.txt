# ========================================
# Lawyer System - Requirements
# ========================================

# Core Framework
# Pin to Django 4.2 LTS for security/stability while preserving compatibility
Django~=4.2.0

# Image Processing (for Profile Pictures, Logos, Receipts)
pillow>=10.2.0

# Task Queue & Scheduling
# ملاحظة: هذه المكتبات مطلوبة فقط إذا كنت تستخدم Celery في الإنتاج
# إذا لم تكن تستخدم Celery، يمكنك تعليقها مؤقتًا
celery>=5.3.6
django-celery-beat>=2.5.0

# Redis Broker (for Celery)
redis>=5.0.1

# ========================================
# PDF Generation (Invoices, Contracts, Reports)
# ========================================
reportlab>=4.1.0          # For PDF generation (most reliable on Windows)
arabic-reshaper>=3.0.0    # Arabic text shaping for PDF
python-bidi>=0.4.2        # RTL support for Arabic in PDF
# xhtml2pdf>=0.2.8       # Alternative
# WeasyPrint>=60.0       # Alternative: requires GTK3 on Windows

# ========================================
# Excel Export (Reports, Financial Data)
# ========================================
openpyxl>=3.1.2      # For .xlsx files
xlsxwriter>=3.1.9    # Alternative Excel writer

# ========================================
# Database (PostgreSQL for Production)
# ========================================
# Uncomment for production deployment:
# psycopg2-binary>=2.9.9

# ========================================
# Environment Variables (Security)
# ========================================
# مطلوب لنقل SECRET_KEY ومتغيرات البيئة للإنتاج
python-decouple>=3.8
# Alternative:
# django-environ>=0.11.2

# ========================================
# Form Rendering (Optional but Recommended)
# ========================================
django-crispy-forms>=2.1
crispy-bootstrap5>=2024.2

# ========================================
# Development & Debugging
# ========================================
# django-debug-toolbar>=4.2.0  # Uncomment for development

# ========================================
# Desktop App Wrapper
# ========================================
# Stage 1 desktop runtime: local WSGI server + native window wrapper
waitress>=3.0.0
pywebview>=5.1

# ========================================
# Production Deployment (Optional)
# ========================================
# gunicorn>=21.2.0    # WSGI server
# whitenoise>=6.6.0   # Static files serving

# ========================================
# Number Formatting & Localization
# ========================================
# Babel provides proper number formatting for different locales
# مفيد لتنسيق الأرقام والعملات حسب اللغة/البلد
babel>=2.14.0
